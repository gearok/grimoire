# The image packages a prebuilt application JAR; source and build tooling do not
# belong in the Docker build context.
**

!Dockerfile

!build
build/*
!build/libs
build/libs/*
!build/libs/grimoire.jar
